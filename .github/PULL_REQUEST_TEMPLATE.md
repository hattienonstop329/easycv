## What this changes

A short summary in your own words. What does the user see now that they didn't before?

## Why

Link to an issue if there is one. If not, a sentence on the underlying problem.

## How to verify

The clicks/keystrokes a reviewer should follow to see the change in action.

## Checks

- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Tested in a browser, not just in my head
- [ ] No new dependencies (or: I justified the new ones above)
- [ ] No backend / accounts / telemetry / outbound network calls introduced

## Screenshots / GIFs

If anything visual changed.
